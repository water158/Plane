<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:gravity="center"
    android:orientation="vertical" >

    <LinearLayout
        android:id="@+id/loading_frame"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:background="@color/pay_white"
        android:gravity="center"
        android:orientation="horizontal" >

        <ImageView
            android:id="@+id/title_load_pb"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="10dp"
            android:layout_marginRight="10dp"
            android:background="@drawable/dialog_loading" />

        <TextView
            android:id="@+id/id_tv_loadingmsg"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginRight="10dp"
            android:text="@string/trade_loading"
            android:textColor="@color/black"
            android:textSize="15sp" />
    </LinearLayout>

    <ListView
        android:id="@+id/orderListview"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_margin="10dip"
        android:background="@color/Transparent"
        android:cacheColorHint="@color/Transparent"
        android:choiceMode="none"
        android:divider="@color/Transparent"
        android:dividerHeight="10dp"
        android:listSelector="@drawable/list_selector_background_disabled"
        android:scrollbarAlwaysDrawVerticalTrack="true"
        android:scrollingCache="true"
        android:visibility="gone" />

    <TextView
        android:id="@+id/empty"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:drawablePadding="20dip"
        android:drawableTop="@drawable/empty_trade_icon"
        android:gravity="center"
        android:padding="15dip"
        android:text="@string/text_no_gmmsg"
        android:textSize="22sp"
        android:visibility="gone" />

</LinearLayout>